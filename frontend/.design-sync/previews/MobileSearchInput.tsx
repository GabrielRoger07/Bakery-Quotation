import { MobileSearchInput } from '@bakery/design-system'

export const Default = () => (
  <div style={{ maxWidth: 400 }}>
    <MobileSearchInput value="fermento" onChange={() => {}} onSearch={() => {}} onClear={() => {}} placeholder="Buscar produtos…" />
  </div>
)
